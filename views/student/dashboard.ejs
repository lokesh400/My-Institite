<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="css/styles.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <link rel="stylesheet" href="https://unpkg.com/aos@2.3.1/dist/aos.css">
    <style>
        html,
        body {
            overflow-x: hidden;
        }

        .card {
            background: linear-gradient(135deg, #1e3a8a, #3b82f6);
            padding: 20px;
            border-radius: 10px;
            text-align: center;
            font-size: 1.5rem;
            font-weight: bold;
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: scale(1.05);
        }

        .btn {
            background: #f59e0b;
            padding: 10px 20px;
            border-radius: 10px;
            font-weight: bold;
            color: black;
            transition: all 0.3s ease;
        }

        .btn:hover {
            background: #d97706;
            transform: scale(1.1);
        }

        .quiz-card {
            background: #4b5563;
            padding: 15px;
            border-radius: 8px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: all 0.3s ease;
        }

        .quiz-card:hover {
            background: #6b7280;
            transform: translateY(-5px);
        }

        .btn-small {
            background: #22c55e;
            padding: 5px 15px;
            border-radius: 5px;
            font-weight: bold;
            transition: all 0.3s ease;
        }

        .btn-small:hover {
            background: #16a34a;
            transform: scale(1.1);
        }
    </style>
</head>

<body class="bg-gray-900 text-white">

    <nav class="bg-gray-800 p-4 flex justify-between items-center shadow-lg">
        <h1 class="text-2xl font-bold">📚 Gamified Learning</h1>
        <div class="flex items-center space-x-6">
            <div class="flex items-center space-x-2">
            <span>🔥 Streak: <%if(participant){%>
                    <%= participant.streak %>
                <%}%>
             </span>
                <span id="streak" class="text-yellow-400 font-bold"> Days</span>
            </div>
            <div class="flex items-center space-x-2">
                <span>💰 Coins: <%if(participant){%>
                    <%= participant.coins %>
                <%}%> </span>
                <span id="coins" class="text-green-400 font-bold"></span>
            </div>
            <a href="/chat">
                <button id="theme-toggle" class="bg-blue-500 px-4 py-2 rounded-lg">Chat</button>
            </a>
            <button onclick="spinWheel()"
                class="bg-yellow-500 px-4 py-2 rounded-lg text-black hover:bg-yellow-400 transition">🎁 Spin &
                Win</button>
        </div>
    </nav>

    <!-- Dashboard Content -->
    <div class="container mx-auto mt-8 p-6">
        <h2 class="text-3xl font-bold mb-6 text-center" data-aos="fade-down">Welcome Back, <span
                class="text-yellow-400">
                <%= currUser.name %>
            </span>👋</h2>

        <!-- Daily Challenges -->
        <div class="mt-8 text-center" data-aos="flip-up">
            <h3 class="text-2xl">🎯 Daily Challenge</h3>
            <p>Complete today’s quiz and earn bonus coins!</p>
            <br>
            <a href="/get/today/streak/quiz">
                <button class="btn">Take Quiz</button>
            </a>
            <a href="/get/all/streak/quiz">
                <button class="btn">Take Quiz</button>
            </a>
        </div>

        <!-- Recommended Quizzes -->
        <div class="mt-10" data-aos="fade-up">
            <h3 class="text-2xl mb-4">🔍 Recommended Quizzes</h3>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div class="quiz-card">
                    <p>🔢 Math Challenge</p>
                    <button class="btn-small">Start</button>
                </div>
                <div class="quiz-card">
                    <p>🧠 Logical Reasoning</p>
                    <button class="btn-small">Start</button>
                </div>
            </div>
        </div>

        <!-- Leaderboard -->
        <div class="mt-10" data-aos="fade-left">
            <h3 class="text-2xl mb-4">🏆 My Class</h3>
            <table class="table-auto w-full bg-gray-800 rounded-lg">
                <tr class="text-yellow-400">
                    <th class="p-3">Rank</th>
                    <th class="p-3">Name</th>
                    <th class="p-3">Coins</th>
                </tr>
                <tr class="hover:bg-gray-700">
                    <td class="p-3">1</td>
                    <td class="p-3">John Doe</td>
                    <td class="p-3">500</td>
                </tr>
                <tr class="hover:bg-gray-700">
                    <td class="p-3">2</td>
                    <td class="p-3">Emma Smith</td>
                    <td class="p-3">480</td>
                </tr>
            </table>
        </div>

        <div class="mt-10" data-aos="fade-right">
            <h3 class="text-2xl mb-4">🏆 Leaderboard</h3>
            <table class="table-auto w-full bg-gray-800 rounded-lg">
                <tr class="text-yellow-400">
                    <th class="p-3">Rank</th>
                    <th class="p-3">Name</th>
                    <th class="p-3">Coins</th>
                </tr>
                <tr class="hover:bg-gray-700">
                    <td class="p-3">1</td>
                    <td class="p-3">John Doe</td>
                    <td class="p-3">500</td>
                </tr>
                <tr class="hover:bg-gray-700">
                    <td class="p-3">2</td>
                    <td class="p-3">Emma Smith</td>
                    <td class="p-3">480</td>
                </tr>
            </table>
        </div>

    </div>

    <script>
        AOS.init();
        gsap.from(".card", { opacity: 0, y: 50, duration: 1, stagger: 0.3 });
    </script>
</body>

</html>